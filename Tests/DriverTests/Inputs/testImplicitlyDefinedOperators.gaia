extern func strcmp(a, b) -> Int

func ==(a, b) {
    return strcmp(a, b) == 0
}

func <(a, b) {
    return strcmp(a, b) < 0
}

print(if "foo" != "foo" then "failure" else "success")
print(if "foo" > "foo" then "failure" else "success")
print(if "foo" <= "foo" then "success" else "failure")
print(if "foo" >= "foo" then "success" else "failure")
